allprojects {
    apply plugin: 'idea'
    apply plugin: 'maven'
}

subprojects {
    group = 'com.thoughtworks.yak'
    version = '0.0.1'

    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.4'
}

